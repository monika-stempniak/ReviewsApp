# Reviews App

React Native example application

Install the dependencies and devDependencies and start the server:

```sh
$ cd reviews-app
$ npm install
$ npm run start
```

For Windows:

```sh
$ npm run android
```

For MacOS:

```sh
$ npm run ios
```
